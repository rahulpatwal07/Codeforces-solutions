#include <bits/stdc++.h>
using namespace std;

#define IOS                  \
    ios::sync_with_stdio(0); \
    cin.tie(0);              \
    cout.tie(0);
#define endl "\n"
#define ll long long

void solve(){
    int n,m;cin>>n>>m;
    for (int i = 0; i < m; i++){
        int l,r;cin>>l>>r;
    }
    bool flag = 1;
    for (int i = 0; i < n; i++){
        cout<<flag;
        flag = !flag;
        
    }
    
}

int32_t main(){
    IOS;
    // ll t;
    // cin>>t;
    // while(t--){
    solve();
    // }

    return 0;
}
