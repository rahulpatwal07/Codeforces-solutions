#include <bits/stdc++.h>

using namespace std;

#define IOS                  \
    ios::sync_with_stdio(0); \
    cin.tie(0);              \
    cout.tie(0);
#define endl "\n"
#define ll long long

void solve(){
    int q;cin>>q;
    if(q<=4){
        cout<<"Bob\n";
    }
    else{
        cout<<"Alice\n";
    }
    
}

int32_t main(){
    IOS;
    ll t;
    cin>>t;
    while(t--){
    solve();
    }
    return 0;
}
